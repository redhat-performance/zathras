#
# Set up terraform (init + workspace) and apply the saved plan to create the
# resources.  On any failure the sub-tasks clean up partial resources via
# tf_delete and abort the play (see tf_bail.yml).
#

- name: Init terraform tasks
  include_tasks: tf_init.yml

- name: terraform apply tasks
  include_tasks: tf_apply.yml
